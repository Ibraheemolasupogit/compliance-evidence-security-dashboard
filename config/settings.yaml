project:
  name: compliance-evidence-security-dashboard
  mode: offline
paths:
  input_dir: data/input
  output_dir: outputs
  reports_dir: reports
defaults:
  timezone: UTC
  date_format: "%Y-%m-%d"
sla_days:
  critical: 7
  high: 14
  medium: 30
  low: 90
  informational: 180
